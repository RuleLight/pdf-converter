# PDF to Word v1.0.0

首个跨平台桌面版本，可在本地将 PDF 转换为 DOCX，不需要安装 Microsoft Word 或 Adobe Acrobat。

## 下载哪个版本

- **PDF-to-Word-Windows-x64.zip**：64 位 Windows 电脑。
- **PDF-to-Word-macOS-Apple-Silicon.zip**：M1、M2、M3、M4、M5 等 Apple Silicon Mac。
- **PDF-to-Word-macOS-Intel.zip**：Intel 处理器 Mac。

下载后请先完整解压 ZIP，再运行其中的 `.exe` 或 `.app`。

## 功能

- 保留版式转换：尽量保留文字、图片、表格和排版。
- 完全一致转换：将每页作为高清图片写入 DOCX。
- OCR 识别：将扫描件识别为可编辑文字。
- 支持指定页码范围和带密码的 PDF。
- 文件完全在本机处理，不会上传到服务器。

## 首次打开提示

程序暂未进行微软或苹果代码签名：

- Windows 可能显示 SmartScreen，选择“更多信息”后可继续运行。
- macOS 请按住 Control 点击 `.app`，然后选择“打开”。

OCR 模式需要另外安装 Tesseract 及所需语言包；普通 PDF 转换不需要。

